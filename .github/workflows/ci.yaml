name: Continuous Integration

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        working-directory: python
        run: |
          uv venv
          uv pip install -e ".[dev]"

      - name: Run ruff check
        working-directory: python
        run: |
          uv run ruff check .

      - name: Run ruff format check
        working-directory: python
        run: |
          uv run ruff format --check .

      - name: Run mypy
        working-directory: python
        run: |
          uv run mypy .

  build:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Build package
        working-directory: python
        run: |
          uv venv
          uv pip install hatch
          uv run hatch build

      - name: Verify package
        working-directory: python
        run: |
          uv pip install dist/*.whl
          uv run python -c "import rowland; print('Package imported successfully')"

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: |
          cd python
          uv venv
          uv pip install -e ".[test]"

      - name: Run integration tests
        env:
          ROWLAND_API_KEY: ${{ secrets.ROWLAND_API_KEY }}
        run: |
          cd python
          uv run pytest
